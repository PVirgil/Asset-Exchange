import Search from "@/components/Search";import AssetTable from "@/components/AssetTable";import {assets,classes} from "@/lib/data";import {compact} from "@/lib/format";import Link from "next/link";
export default function Home(){return <><section className="hero"><div className="eyebrow">THE UNIVERSAL ASSET TERMINAL</div><h1>Track the value<br/>of <em>everything.</em></h1><p>One intelligence layer for public markets, digital assets, real estate, private markets and the world's collectible economy.</p><Search/><div className="hint">Try: AAPL · Bitcoin · Gold · Daytona · Manhattan</div></section><section className="stats"><div><small>ASSET UNIVERSE</small><strong>All markets</strong></div><div><small>DEMO ASSETS</small><strong>{assets.length}</strong></div><div><small>ASSET CLASSES</small><strong>{classes.length}</strong></div><div><small>DATA MODEL</small><strong>Universal</strong></div></section><section className="section"><div className="sectionHead"><div><span className="eyebrow">MARKET PULSE</span><h2>Across the exchange</h2></div><Link href="/markets">Explore markets →</Link></div><AssetTable items={assets}/></section><section className="section grid3">{classes.slice(0,6).map(c=><Link className="classCard" href={`/markets?class=${encodeURIComponent(c)}`} key={c}><span>{c}</span><strong>{assets.filter(a=>a.assetClass===c).length}</strong><p>Explore tracked {c.toLowerCase()} assets, pricing signals and valuation confidence.</p></Link>)}</section><section className="cta"><span className="eyebrow">ONE SCHEMA. EVERY ASSET.</span><h2>Public price or private estimate,<br/>Asset Exchange speaks both.</h2><p>Designed to distinguish executable market prices from modeled AE Estimated Values.</p></section></>}
