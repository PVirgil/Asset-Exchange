import AssetTable from "@/components/AssetTable";import {assets,classes} from "@/lib/data";
export default function Markets({searchParams}:{searchParams:{class?:string}}){const chosen=searchParams.class;const items=chosen?assets.filter(a=>a.assetClass===chosen):assets;return <section className="page"><span className="eyebrow">MARKETS</span><h1>Universal market map</h1><p className="lede">Move across liquid markets and hard-to-price assets without changing your research workflow.</p><div className="filters"><a href="/markets">All</a>{classes.map(c=><a key={c} href={`/markets?class=${encodeURIComponent(c)}`}>{c}</a>)}</div><AssetTable items={items}/></section>}
