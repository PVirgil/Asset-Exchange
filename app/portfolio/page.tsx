import {assets} from "@/lib/data";import {money} from "@/lib/format";
const holdings=[{a:assets[0],qty:12},{a:assets[1],qty:.18},{a:assets[4],qty:1}];
export default function Portfolio(){const total=holdings.reduce((s,h)=>s+h.a.price*h.qty,0);return <section className="page"><span className="eyebrow">PORTFOLIO</span><h1>Your whole balance sheet</h1><p className="lede">A demonstration of the unified portfolio layer. Connect authentication and brokerage/wallet/property sources for production.</p><div className="heroMetric"><small>DEMO NET ASSET VALUE</small><strong>{money(total)}</strong></div><div className="panel">{holdings.map(h=><div className="holding" key={h.a.id}><div><b>{h.a.symbol}</b><span>{h.a.name}</span></div><span>{h.qty} units</span><strong>{money(h.a.price*h.qty)}</strong></div>)}</div></section>}
