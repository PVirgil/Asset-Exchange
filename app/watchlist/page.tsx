import AssetTable from "@/components/AssetTable";import {assets} from "@/lib/data";export default function Watchlist(){return <section className="page"><span className="eyebrow">WATCHLIST</span><h1>Assets on your radar</h1><p className="lede">Demo watchlist. The included database schema supports per-user persisted watchlists with row-level security.</p><AssetTable items={assets.slice(0,5)}/></section>}
