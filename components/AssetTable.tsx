import Link from "next/link";import {Asset} from "@/lib/types";import {money,pct,compact} from "@/lib/format";
export default function AssetTable({items}:{items:Asset[]}){return <div className="tableWrap"><table><thead><tr><th>Asset</th><th>Class</th><th>Price / AE value</th><th>24h / period</th><th>Market value</th><th>Confidence</th></tr></thead><tbody>{items.map(a=><tr key={a.id}><td><Link href={`/asset/${a.slug}`}><b>{a.symbol}</b><span className="sub">{a.name}</span></Link></td><td><span className="pill">{a.assetClass}</span></td><td>{money(a.price,a.currency)}</td><td className={a.change>=0?"up":"down"}>{pct(a.change)}</td><td>{a.marketCap?"$"+compact(a.marketCap):"Modeled"}</td><td>{a.confidence}%</td></tr>)}</tbody></table></div>}
